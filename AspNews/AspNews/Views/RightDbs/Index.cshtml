@model IEnumerable<AspNews.Models.cjl.RightDb>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RightName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RightName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RightID }) |
            @Html.ActionLink("Details", "Details", new { id=item.RightID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RightID })
        </td>
    </tr>
}

</table>
